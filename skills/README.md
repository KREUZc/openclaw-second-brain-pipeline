# Skills

These skills are copies of local OpenClaw skills, sanitized for publication.

- `zettel-spark-catcher/` — ask 2–3 guiding questions for a fresh idea and retain raw inbox notes.
- `zettel-permanent-card-forger/` — forge atomic permanent notes (requires tags 2–4, includes summary).
- `mycrew-diary-skill/` — daily diary writer (schedule example is platform-specific; adapt as needed).
- `clipping-router/` — generic clipping routing skill (11_Clipping → project clips + clips_log updates).

> NOTE: This repository does not modify your existing local skills. It provides a publishable snapshot.
